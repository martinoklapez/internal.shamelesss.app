import { MissiveIntegrationPanel } from '@/components/missive-integration-panel'

export const metadata = {
  title: 'Shamelesss Pipeline — Missive',
  description: 'Creator Pipeline CRM context for Missive conversations',
}

export default function MissiveIntegrationPage() {
  return <MissiveIntegrationPanel />
}
